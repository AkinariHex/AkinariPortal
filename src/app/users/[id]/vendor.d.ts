// react-copy-to-clipboard ships no type declarations and has no @types package.
// Declare it as an untyped module so it can be imported under strict TypeScript.
declare module "react-copy-to-clipboard";
